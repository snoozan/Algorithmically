function split(arr)
    if(size(arr, x) < 2)
       return arr
    
#Mergesort in Julia! 

#set this to always be an Int
function split(arr)
    if(size(arr, x) < 2)
        return arr
    middle = size(arr, x)//2
end



arr = [5,6,7]
split(arr)
